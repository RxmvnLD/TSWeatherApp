import { useState } from "react";

const useGetCityPicture = () => {
  const [cityPicture, setCityPicture] = useState({});
  return <div>useGetCityPicture</div>;
};

export default useGetCityPicture;
